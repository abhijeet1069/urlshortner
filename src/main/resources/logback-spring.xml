<?xml version="1.0" encoding="UTF-8"?>

<configuration>

    <!-- =========================
         Console Logging
         ========================= -->

    <appender name="CONSOLE"
              class="ch.qos.logback.core.ConsoleAppender">

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] [%X{requestId}] %logger{36} - %msg%n</pattern>
        </encoder>

    </appender>


    <!-- =========================
         Application File Logging
         ========================= -->

    <appender name="APPLICATION_FILE"
              class="ch.qos.logback.core.rolling.RollingFileAppender">

        <file>logs/application.log</file>

        <rollingPolicy
                class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">

            <!-- New file every day -->
            <!-- Also create a new file if it exceeds 10MB -->
            <fileNamePattern>
                logs/application.%d{yyyy-MM-dd}.%i.log.gz
            </fileNamePattern>

            <maxFileSize>10MB</maxFileSize>

            <!-- Keep logs for 30 days -->
            <maxHistory>30</maxHistory>

            <!-- Maximum total size of archived logs -->
            <totalSizeCap>1GB</totalSizeCap>

        </rollingPolicy>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] [%X{requestId}] %logger{36} - %msg%n</pattern>
        </encoder>

    </appender>


    <!-- =========================
         Root Logger
         ========================= -->

    <root level="INFO">

        <appender-ref ref="CONSOLE"/>

        <appender-ref ref="APPLICATION_FILE"/>

    </root>


    <!-- =========================
         Your Application
         ========================= -->

    <logger name="com.backend" level="DEBUG"/>

</configuration>